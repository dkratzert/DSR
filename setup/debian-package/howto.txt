-control and postinst in the DEBIAN directory control the behavior
-copy content of debian-package to /usr/src/packages/BUILD
-rename it to dsr. Important! No version number!
-the final directory structure must exist in [..]/BUILD/dsr
-execute "dpkg-deb --build dsr"
-rename dsr.deb to dsr-[version].deb
